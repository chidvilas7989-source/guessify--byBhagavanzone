{
  "name": "guessify",
  "version": "5.0.0",
  "description": "Real-time multiplayer music guessing game with ordered host rotation, 30s grace period auto-skip, 60-second clip playback, song preloading, REST API, and premium UI/UX",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": [
    "game",
    "multiplayer",
    "music",
    "socket.io",
    "real-time",
    "host-rotation",
    "grace-period",
    "track-selection",
    "guessing-game",
    "express"
  ],
  "author": "Bhagavan & Team — BhagavanZone",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  },
  "features": {
    "roundDuration": "180s (3 minutes)",
    "gracePeriod": "30s auto-skip if host is idle",
    "trackDuration": "60s clip per round",
    "hostRotation": "in-order, all players complete one cycle before repeating",
    "songPreload": "members preload audio when host selects a song",
    "restApi": [
      "GET /api/health",
      "GET /api/songs",
      "GET /api/rooms",
      "GET /api/rooms/:code"
    ]
  }
}